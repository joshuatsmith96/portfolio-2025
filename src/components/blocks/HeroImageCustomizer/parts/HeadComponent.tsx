import { Box, Typography } from "@mui/material"

export const HeadComponent = () => {
    return (
        <Box color={'white'} sx={{ order: 1, textAlign: 'center' }}><Typography>[desktop]Head Component</Typography></Box>
    )
}